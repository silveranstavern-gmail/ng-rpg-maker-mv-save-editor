import { Routes } from '@angular/router';
import { SaveEditorComponent } from './pages/save-editor/save-editor.component';

export const routes: Routes = [{ path: '', component: SaveEditorComponent }];
